package others;

public class stringBuilder {
	public static void main(String[] args) {
		// StringBuilder sb = new StringBuilder("Megha");
		// // System.out.println(sb);

		// // System.out.println(sb.charAt(0));

		// // set char at any index using :
		// // var_name.setCharAt(index, value yo u want to change at that index)

		// sb.setCharAt(0, 'D'); // replace
		// System.out.println(sb);

		// sb.insert(2, 'e');
		// System.out.println(sb); // insert using index

		// sb.delete(3, 6);
		// System.out.println(sb); // delete using indexing (starting index, ending
		// index+1)

		// sb.append('p');
		// sb.append('a');
		// System.out.println(sb); // append value at the end of string.

		// System.out.println(sb.length()); // length (no. of element present)

		// // Reversing a STRING - 1 :
		// String Str = "Hello";
		// String newStr = " ";

		// for (int i = Str.length() - 1; i >= 0; i--) {
		// newStr = newStr + Str.charAt(i);
		// }
		// System.out.println(newStr);

		// // Reversing a STRING - 2 :
		// StringBuilder sb = new StringBuilder("HelloWorld");

		// for (int i = 0; i < sb.length() / 2; i++) {
		// int front = i;
		// int back = sb.length() - i - 1;

		// char frontChar = sb.charAt(front);
		// char backChar = sb.charAt(back);

		// sb.setCharAt(front, backChar);
		// sb.setCharAt(back, frontChar);
		// }

		// System.out.println(sb);
	}
}
