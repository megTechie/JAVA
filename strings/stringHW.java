package strings;

import java.util.*;

public class stringHW {
	public static void main(String[] args) {
		// Q1 : Take an array of Strings input from the user & find the cumulative
		// (combined) length of all those strings.

		// Scanner sc = new Scanner(System.in);

		// System.out.print("Enter Size : ");
		// int size = sc.nextInt();
		// sc.nextLine();

		// String arr[] = new String[size];
		// int len = 0;

		// for (int i = 0; i < size; i++) {
		// System.out.print("Enter element at [" + i + "] index : ");
		// arr[i] = sc.nextLine();
		// len = len + arr[i].length();
		// }
		// System.out.println("Combined length of all strings : " + len);

		// sc.close();

		// Q2 : Input a string from the user. Create a new string called ‘result’ in
		// which you will replace the letter ‘e’ in the original string with letter ‘i’.

		// Scanner sc = new Scanner(System.in);

		// System.out.print("Enter String : ");
		// String str = sc.nextLine();

		// String result = "";

		// for (int i = 0; i < str.length(); i++) {
		// if (str.charAt(i) == 'e') {
		// result = result + 'i';
		// } else {
		// result += str.charAt(i);
		// }
		// }

		// System.out.println(result);

		// sc.close();

		// Q3 : Input an email from the user. You have to create a username from the
		// email by deleting the part that comes after ‘@’. Display that username to the
		// user.

		// Scanner sc = new Scanner(System.in);

		// System.out.print("Enter your email : ");
		// String email = sc.nextLine();
		// String name = "";

		// for (int i = 0; i < email.length(); i++) {
		// if (email.charAt(i) == '@') {
		// break;
		// } else {
		// name = name + email.charAt(i);
		// }
		// }

		// System.out.println("Username : " + name);
		// sc.close();
	}
}
